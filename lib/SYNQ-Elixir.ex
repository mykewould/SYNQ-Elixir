defmodule SynqElixir do
  @moduledoc """
  SynqElixir is an elixir SDK to Synq.fm through an elixir application.

  You need to set `API_KEY` environment variable to use SynqElixir with our api.
  """
end
